# SofterPlease 项目状态评审（2026-04-18）

## 1) 功能开发状态

### 已实现（可见代码）
- 后端已具备基础业务链路：用户、家庭、会话、情绪事件、反馈、报表、目标、埋点与系统健康接口。
- 已接入实时链路：`/v1/realtime/ws` 与 `POST /v1/sessions/{session_id}/analyze`。
- Web 与 Flutter 端均有可运行壳层和页面逻辑，并调用后端 API。

### 主要风险/未闭环点
- 文档显示“下一步”仍包含：
  1. PostgreSQL 全量迁移脚本补齐；
  2. 移动端接入真实 API 与实时反馈链路；
  3. 接入模型服务（ASR/声纹/情绪）并压测。
- 当前后端默认数据库仍为 SQLite（不是生产化默认）。
- 本地测试无法直接跑通（缺少运行依赖）。

## 2) 模型数据与语料状态

### 结论
- 仓库内未发现训练数据集/语料目录；
- 未发现模型权重文件（`.pth/.pt/.ckpt/.onnx`）；
- 也未发现训练脚本或训练流水线文件。

### 代码侧佐证
- 情绪模型与声纹模型设计为“若有权重则加载，否则退化为规则引擎”。
- 该设计表明当前代码主要是推理壳与规则兜底，并非完整训练闭环。

## 3) 模型训练进度

### 结论
- 代码仓库中看不到“训练进行中/已完成训练”的证据。
- 无训练日志、无评估产物、无 checkpoint 版本记录。
- 因此只能判定：训练工作尚未体现在该代码仓库中（或在外部私有环境进行，仓库未同步）。

## 4) 模型效果评估状态

### 结论
- 未见标准评估指标（如情绪分类 F1/Accuracy、声纹 EER、ASR WER/CER）。
- 未见离线评测报告或 A/B 实验报告。
- 当前可确认的效果仅限“功能可跑的规则/轻量推理框架”，无法给出可信的模型效果结论。

## 5) 建议的下一步（按优先级）

1. **先把可运行性补齐**：补全 `backend/requirements.txt` 与测试环境，确保 CI 可执行。
2. **补数据资产目录规范**：新增 `data/README.md`，明确语料来源、授权、标注规范与切分策略。
3. **补训练流水线**：加入 `training/`（数据处理、训练、评估、导出）与实验追踪。
4. **补评估基线**：至少输出 emotion、speaker、ASR 三块离线指标与验证集定义。
5. **补部署契约**：将模型版本、阈值、回滚策略纳入部署文档与 API 响应。
